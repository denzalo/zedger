# Python venv
.venv/

# Byte-code / caches
__pycache__/
*.py[cod]

# Environment / editor
.env
*.env.*         # .env.local, .env.prod, etc.
!.env.example   # keep the public template tracked
.vscode/
.idea/

# macOS junk
.DS_Store

# Ignore everything in the instance folder (runtime data)
instance/*

# …but keep the folder itself so new clones have it
!instance/.gitkeep